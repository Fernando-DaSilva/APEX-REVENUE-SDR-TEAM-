# SKILLS & SPECIALIZATIONS — Atlas (AI Multi-Agent & RAG Lead)

- LangChain (`langchain-core`) & LangGraph (`StateGraph`) Multi-Agent Orchestration
- Persistent Checkpointing (`AsyncPostgresSaver`) & Thread Isolation
- Instructor + Pydantic v2 Structured Data Extraction
- Hybrid RAG Vector Search (`pgvector` HNSW + `tsvector` BM25 RRF)
- LLM Router Fallback Optimization & LangSmith Telemetry
